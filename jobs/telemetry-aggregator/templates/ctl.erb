#!/bin/bash

RUN_DIR=/var/vcap/sys/run/telemetry-aggregator
LOG_DIR=/var/vcap/sys/log/telemetry-aggregator
PIDFILE=${RUN_DIR}/pid

case $1 in

  start)
    mkdir -p $RUN_DIR $LOG_DIR
    chown -R vcap:vcap $RUN_DIR $LOG_DIR

    echo $$ > $PIDFILE

    cd /var/vcap/packages/fluent-bit

    exec /var/vcap/packages/fluent-bit/bin/fluent-bit \
      --config=/var/vcap/jobs/telemetry-aggregator/config/fluent.cnf \
      >>  $LOG_DIR/telemetry-aggregator.stdout.log \
      2>> $LOG_DIR/telemetry-aggregator.stderr.log

    ;;

  stop)
    kill -9 `cat $PIDFILE`
    rm -f $PIDFILE

    ;;

  *)
    echo "Usage: ctl {start|stop}" ;;

esac
